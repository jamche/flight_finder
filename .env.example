# ── SerpApi Google Flights ───────────────────────────────────────────────────
# Free tier: 100 searches/month, no credit card required.
# Register at https://serpapi.com -> Dashboard -> copy your API Key.
SERPAPI_KEY=your_serpapi_key_here

# ── Search Parameters ────────────────────────────────────────────────────────
# Origin airport IATA code
ORIGIN=YYZ

# Destination IATA codes – metro codes search all airports in the city
DEST_JAPAN=TYO       # Tokyo (covers NRT and HND)
DEST_TAIWAN=TPE      # Taipei Taoyuan
DEST_THAILAND=BKK    # Bangkok (covers BKK and DMK)

# Departure dates to search.
# Option A – explicit comma-separated dates (YYYY-MM-DD):
# DEPARTURE_DATES=2026-10-23,2026-10-24,...,2026-11-08
#
# Option B – days ahead from today (used when DEPARTURE_DATES is not set):
DAYS_AHEAD=30,60,90

# Optional: exclude flights that depart before a given time on one specific date.
# Format: EARLIEST_DEP_DATE=YYYY-MM-DD, EARLIEST_DEP_TIME=HH:MM (24h, local airport time).
# Example below keeps only Oct 23 flights after 7 pm ET.
EARLIEST_DEP_DATE=
EARLIEST_DEP_TIME=

# Return dates – for return-leg one-way and round-trip searches.
# Leave blank to run outbound-only. roundtrip/return are skipped when not set.
RETURN_DATES=

# Trip types to include (comma-separated: outbound, return, roundtrip)
TRIP_TYPES=outbound,return,roundtrip

# Number of passengers
ADULTS=1

# Max flight results returned per route per date
MAX_RESULTS=5

# Currency for prices
CURRENCY=CAD

# ── SMTP Email ───────────────────────────────────────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=you@gmail.com
SMTP_PASS=your_gmail_app_password
EMAIL_FROM=you@gmail.com
EMAIL_TO=recipient@example.com
